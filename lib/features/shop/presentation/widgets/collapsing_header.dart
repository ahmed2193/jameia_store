// Obsolete. The faked-from-scroll-px collapsing header was replaced by a native
// `SliverPersistentHeader` collapse — see `ShopHeroHeader` + `HeaderChromeRow`
// in `shop_hero.dart`. This file is intentionally left empty (no longer imported
// anywhere); it can be removed once tooling allows the delete.
