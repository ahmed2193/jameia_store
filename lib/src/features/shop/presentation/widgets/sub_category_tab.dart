// Obsolete. The per-tab ScrollablePositionedList sub-category page was replaced
// by a single CustomScrollView with scroll-spy — the body now lives directly in
// `_ShopMenuState` in `shop_screen.dart` (sub-tabs are a jump/swap control, not a
// TabBarView). This file is intentionally left empty (no longer imported
// anywhere); it can be removed once tooling allows the delete.
