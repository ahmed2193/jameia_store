// GENERATED-BY-EXTRACTION — do not hand-edit codepoints.
//
// Real KeeTa icon glyphs resolved from the bundled `wm_c_iconfont.ttf`
// (cmap format 12 + post v2 glyph names) extracted from the decoded KeeTa APK.
// Family is registered as 'KeetaIcon' in pubspec.yaml.
//
// Codepoints below are the ACTUAL glyph code points in the font — verified by
// parsing the font's cmap/post tables, not guessed. Glyphs whose semantics could
// not be resolved confidently (plus, minus, scan/QR) are intentionally OMITTED;
// see the note at the bottom of this file.
import 'package:flutter/widgets.dart';

/// Icon constants over the real KeeTa `wm_c_iconfont` ('KeetaIcon' family).
///
/// Original glyph names (pinyin/english from the font's `post` table) are noted
/// in comments so feature code can trace each constant back to the source glyph.
class KeetaIcons {
  KeetaIcons._();

  static const String fontFamily = 'KeetaIcon';

  // ── Search ────────────────────────────────────────────────────────────────
  /// magnifier / search (`wm_c_iconfont_27fangdajing`)
  static const IconData search = IconData(0xe017, fontFamily: fontFamily);
  /// clear search field (`wm_c_iconfont_search_clear`)
  static const IconData searchClear = IconData(0xe05c, fontFamily: fontFamily);

  // ── Close ───────────────────────────────────────────────────────────────────
  /// close (`wm_c_iconfont_26guanbi`)
  static const IconData close = IconData(0xe016, fontFamily: fontFamily);
  /// close, alt weight (`wm_c_iconfont_76guanbi`)
  static const IconData closeAlt = IconData(0xe030, fontFamily: fontFamily);
  /// small close (`wm_c_iconfont_close_small`)
  static const IconData closeSmall = IconData(0xe043, fontFamily: fontFamily);

  // ── Location / address ────────────────────────────────────────────────────
  /// location pin (`wm_c_iconfont_14dingwei`)
  static const IconData location = IconData(0xe00b, fontFamily: fontFamily);
  /// location pin, alt (`wm_c_iconfont_35dingwei`)
  static const IconData locationAlt = IconData(0xe01d, fontFamily: fontFamily);
  /// location, outline (`wm_c_iconfont_location`)
  static const IconData locationOutline = IconData(0xe05b, fontFamily: fontFamily);
  /// store location (`wm_c_iconfont_store_location`)
  static const IconData storeLocation = IconData(0xe03d, fontFamily: fontFamily);
  /// address (`wm_c_iconfont_dizhi`)
  static const IconData address = IconData(0xe057, fontFamily: fontFamily);

  // ── Chat / contact ──────────────────────────────────────────────────────────
  /// chat / IM (`wm_c_iconfont_28im`)
  static const IconData chat = IconData(0xe018, fontFamily: fontFamily);
  /// chat with rider (`wm_c_iconfont_29im_lianxiqishou`)
  static const IconData chatRider = IconData(0xe019, fontFamily: fontFamily);
  /// chat with merchant (`wm_c_iconfont_30im_lxsj`)
  static const IconData chatMerchant = IconData(0xe01a, fontFamily: fontFamily);
  /// phone (`wm_c_iconfont_15dianhua`)
  static const IconData phone = IconData(0xe00c, fontFamily: fontFamily);
  /// contact merchant (`wm_c_iconfont_16lianxishangjia`)
  static const IconData contactMerchant = IconData(0xe00d, fontFamily: fontFamily);
  /// contact rider (`wm_c_iconfont_17lianxiqishou`)
  static const IconData contactRider = IconData(0xe00e, fontFamily: fontFamily);
  /// customer service (`wm_c_iconfont_68kefu`)
  static const IconData customerService = IconData(0xe02e, fontFamily: fontFamily);
  /// medical consult (`wm_c_iconfont_55zixunyisheng`)
  static const IconData consultDoctor = IconData(0xe02b, fontFamily: fontFamily);

  // ── Navigation arrows ───────────────────────────────────────────────────────
  /// back / arrow-left (`wm_c_iconfont_jiantou_zuo`) — auto-mirrors in RTL via Icon
  static const IconData back = IconData(0xe036, fontFamily: fontFamily);
  /// arrow-right (`wm_c_iconfont_jiantou_you`)
  static const IconData arrowRight = IconData(0xe035, fontFamily: fontFamily);
  /// arrow-down (`wm_c_iconfont_jiantou_xia`)
  static const IconData arrowDown = IconData(0xe034, fontFamily: fontFamily);
  /// arrow-up (`wm_c_iconfont_arrow_up`)
  static const IconData arrowUp = IconData(0xe041, fontFamily: fontFamily);
  /// small arrow-right (`wm_c_iconfont_arrow_right_small`)
  static const IconData arrowRightSmall = IconData(0xe042, fontFamily: fontFamily);
  /// small arrow-down (`wm_c_iconfont_arrow_down_small`)
  static const IconData arrowDownSmall = IconData(0xe044, fontFamily: fontFamily);
  /// small arrow-up (`wm_c_iconfont_arrow_up_small`)
  static const IconData arrowUpSmall = IconData(0xe045, fontFamily: fontFamily);

  // ── Share / social ──────────────────────────────────────────────────────────
  /// share (`wm_c_iconfont_13fenxiang`)
  static const IconData share = IconData(0xe00a, fontFamily: fontFamily);
  /// reward / tip (`wm_c_iconfont_24dashang`)
  static const IconData reward = IconData(0xe014, fontFamily: fontFamily);

  // ── Rating / favorite ───────────────────────────────────────────────────────
  /// star selected (`wm_c_iconfont_star_select`)
  static const IconData star = IconData(0xe038, fontFamily: fontFamily);
  /// favorite / heart-collect (`wm_c_iconfont_2shoucang`)
  static const IconData favorite = IconData(0xe000, fontFamily: fontFamily);
  /// like / thumbs-up (`wm_c_iconfont_51zan`)
  static const IconData like = IconData(0xe028, fontFamily: fontFamily);
  /// dislike (`wm_c_iconfont_50cai`)
  static const IconData dislike = IconData(0xe027, fontFamily: fontFamily);
  /// rank (`wm_c_iconfont_rank`)
  static const IconData rank = IconData(0xe03c, fontFamily: fontFamily);

  // ── Cart / commerce ─────────────────────────────────────────────────────────
  /// shopping cart (`wm_c_iconfont_3gouwuche`)
  static const IconData cart = IconData(0xe033, fontFamily: fontFamily);
  /// product (`wm_c_iconfont_s01-shangpin`)
  static const IconData product = IconData(0xe05a, fontFamily: fontFamily);
  /// store (`wm_c_iconfont_store`)
  static const IconData store = IconData(0xe03e, fontFamily: fontFamily);
  /// shop / merchant (`wm_c_iconfont_dianpu`)
  static const IconData shop = IconData(0xe058, fontFamily: fontFamily);
  /// merchant alt (`wm_c_iconfont_47shangjia`)
  static const IconData merchant = IconData(0xe026, fontFamily: fontFamily);
  /// pay (`wm_c_iconfont_20fukuan`)
  static const IconData pay = IconData(0xe011, fontFamily: fontFamily);
  /// refund (`wm_c_iconfont_21tuikuan`)
  static const IconData refund = IconData(0xe012, fontFamily: fontFamily);
  /// fast refund (`wm_c_iconfont_25jisutuikuan`)
  static const IconData fastRefund = IconData(0xe015, fontFamily: fontFamily);
  /// orders (`wm_c_iconfont_45dingdan`)
  static const IconData orders = IconData(0xe024, fontFamily: fontFamily);
  /// order again (`wm_c_iconfont_42zailai1dan`)
  static const IconData orderAgain = IconData(0xe022, fontFamily: fontFamily);
  /// confirm receipt (`wm_c_iconfont_43querenshouhuo`)
  static const IconData confirmReceipt = IconData(0xe023, fontFamily: fontFamily);

  // ── Filter / more ───────────────────────────────────────────────────────────
  /// filter (`wm_c_iconfont_shaixuan`)
  static const IconData filter = IconData(0xe059, fontFamily: fontFamily);
  /// more, horizontal (`wm_c_iconfont_77gengduo_hengxiang`)
  static const IconData more = IconData(0xe031, fontFamily: fontFamily);
  /// more, vertical (`wm_c_iconfont_78gengduo_shuxiang`)
  static const IconData moreVertical = IconData(0xe032, fontFamily: fontFamily);

  // ── Time / delivery ─────────────────────────────────────────────────────────
  /// time / clock (`wm_c_iconfont_time`)
  static const IconData time = IconData(0xe03f, fontFamily: fontFamily);
  /// delivery time (`wm_c_iconfont_41peisongshijian`)
  static const IconData deliveryTime = IconData(0xe021, fontFamily: fontFamily);
  /// delivery (`wm_c_iconfont_delivery`)
  static const IconData delivery = IconData(0xe039, fontFamily: fontFamily);
  /// urge order (`wm_c_iconfont_39cuidan`)
  static const IconData urgeOrder = IconData(0xe01f, fontFamily: fontFamily);
  /// find rider (`wm_c_iconfont_19zhaorenfu`)
  static const IconData findRider = IconData(0xe010, fontFamily: fontFamily);

  // ── Camera / media ──────────────────────────────────────────────────────────
  /// camera (`wm_c_iconfont_xiangji`)
  static const IconData camera = IconData(0xe04d, fontFamily: fontFamily);
  /// camera / video (`wm_c_iconfont_shexiang`)
  static const IconData videoCamera = IconData(0xe050, fontFamily: fontFamily);
  /// photo album (`wm_c_iconfont_54xiangce`)
  static const IconData album = IconData(0xe029, fontFamily: fontFamily);
  /// image (`wm_c_iconfont_tupian`)
  static const IconData image = IconData(0xe04e, fontFamily: fontFamily);

  // ── Status / info ───────────────────────────────────────────────────────────
  /// info / warning (`wm_c_iconfont_31jingshi`)
  static const IconData info = IconData(0xe01b, fontFamily: fontFamily);
  /// question (`wm_c_iconfont_32wenhao`)
  static const IconData help = IconData(0xe01c, fontFamily: fontFamily);
  /// notice (`wm_c_iconfont_notice`)
  static const IconData notice = IconData(0xe03b, fontFamily: fontFamily);
  /// alert / reminder (`wm_c_iconfont_38tixing`)
  static const IconData alert = IconData(0xe01e, fontFamily: fontFamily);
  /// confirm / done (`wm_c_iconfont_confirm`)
  static const IconData confirm = IconData(0xe040, fontFamily: fontFamily);
  /// edit (`wm_c_iconfont_63bianji`)
  static const IconData edit = IconData(0xe02c, fontFamily: fontFamily);
  /// delete (`wm_c_iconfont_67shanchu`)
  static const IconData delete = IconData(0xe02d, fontFamily: fontFamily);
  /// food safety (`wm_c_iconfont_food_safe`)
  static const IconData foodSafe = IconData(0xe03a, fontFamily: fontFamily);
  /// flame / hot (`wm_c_iconfont_huomiao`)
  static const IconData flame = IconData(0xe055, fontFamily: fontFamily);

  // ───────────────────────────────────────────────────────────────────────────
  // OMITTED — no matching glyph found in wm_c_iconfont.ttf (would be a guess):
  //   • plus  / add   — no `jia`/`add` glyph in the font cmap.
  //   • minus / remove — no `jian`/`minus`/`remove` glyph in the font cmap.
  //   • scan / QR      — no `saoyisao`/`scan`/`qrcode` glyph (use `camera` for
  //                      capture flows, or the `scan_qrcode` PNG in
  //                      KeetaAssets.mineScanQrCode for the Mine scan entry).
  // Use Material `Icons.add` / `Icons.remove` for stepper +/- controls.
}
